# Dockerfile per Development del Frontend React
# Utilizzato per hot reload durante lo sviluppo

FROM node:18-alpine

# Installa curl per health checks
RUN apk add --no-cache curl

# Imposta la directory di lavoro
WORKDIR /app

# Copia package.json e package-lock.json
COPY package*.json ./

# Installa tutte le dipendenze (incluse quelle di sviluppo)
RUN npm ci

# Copia il resto del codice sorgente
COPY . .

# Espone la porta 3000 (Vite dev server)
EXPOSE 3000

# Health check
HEALTHCHECK --interval=30s --timeout=3s --start-period=5s --retries=3 \
    CMD curl -f http://localhost:3000/ || exit 1

# Comando per avviare il server di sviluppo
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
